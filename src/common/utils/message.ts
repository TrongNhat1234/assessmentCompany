const loginMessage = (address: string, nonce: string) => {
  return `Welcome to the best marketplace Padisea\nYou are going to login with:\n\tAddress: ${address}\n\tNonce: ${nonce}`;
};

export { loginMessage };
